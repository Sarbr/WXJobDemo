server:
  port: 8888

spring:
  # thymeleaf 模板配置
  thymeleaf:
    check-template-location: true
    suffix: .html
    mode: HTML
    encoding: UTF-8
    prefix: classpath:templates/
    cache: true
app-code: code
wei-xin-url: url